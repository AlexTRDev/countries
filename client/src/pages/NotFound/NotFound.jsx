import React from 'react';
import styled from 'styled-components';

const NotFound = () => {
	return <NotFoundStyle>NotFound</NotFoundStyle>;
};

export const NotFoundStyle = styled.div``;

export default NotFound;
